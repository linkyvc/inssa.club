import styled from 'styled-components';

export const Input = styled.input`
  font-size: 18.5px;
  font-weight: 500;
  padding: 16px 20px;
  border: 3px solid #f4f4f4;
  border-radius: 8px;
  background: #ffffff;

  &::placeholder {
    color: #adb5bd;
    font-weight: 400;
  }
`;
